#include "machine.h"

AXSTATUS AXCreateMachine(
	_In_ PAXMACHINE_CONFIG config,
	_Out_ PAXMACHINE handle
) {
	UNREFERENCED_PARAMETER(config);
	UNREFERENCED_PARAMETER(handle);
	DbgBreakPoint();
	return STATUS_SUCCESS;
}

