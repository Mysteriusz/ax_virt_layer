Urgent:

[x] i64 support for destination mov instruction

[] tblock error management
return error codes not bools and allow axres return for 'ir.desc.call functions'

Later:

[] i64 support for destination register reordering
for IR (see: i64_ir_to_raw assertion)

Done:

[] Remove the 'tblock.base' field

[] CPU Pointer buffer of currently active memory pointers

[] Spill managment in 'cpu_alloc_role_reg'
	DONE - Spill buffer has to be managed by the cpu structure
	  but cannot be intertwined with register allocation since ir is the only
	  interface that requiers register allocation for tblock emittion
	DONE - Scrap register states for the cpu since cpu doesn`t care about that and put them
	  into some ir/tblock cpu primitive

